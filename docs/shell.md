## shell

```bash
执行shell命令

Usage:
  ergo shell [flags]

Flags:
      --cmd string    shell命令 (default "whoami")
  -h, --help          help for shell
      --ip strings    需要执行shell的ip (default [11.11.11.111])
      --pass string   管理员密码 (default "vagrant")
      --pk string     管理员私钥
      --user string   管理员 (default "root")
```

